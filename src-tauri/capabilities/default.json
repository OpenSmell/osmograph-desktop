{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main window capability. Grants the standard Tauri 2 core defaults so the frontend can listen for real-time events (live readings) and use core APIs. Custom commands registered via invoke_handler remain allowed by default.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
